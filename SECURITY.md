# Security

- Never commit secrets. This repo gitignores `.env*`.
- Do not include hostnames, IPs, Discord IDs, or personal identifiers in docs.
- All network access should go through an explicit allowlist.
- Enforce spend caps for any paid APIs / model calls.
